# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'Top Comments' do
  use_frameworks!
  
  platform :ios, '9.0'
  pod "VK-ios-sdk"
  pod "Nuke"
end
